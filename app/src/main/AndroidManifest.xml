<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mamunsproject.youtubekids">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        tools:replace="android:icon"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YoutubeKids">
        <activity android:name=".Activity.Hindi_Cartoon"/>
        <activity android:name=".Activity.MrBeanCartoon" />
        <activity android:name=".Activity.BanglaCartoon" />
        <activity android:name=".Activity.Oggy_and_coakroaches" />
        <activity android:name=".Activity.Ben10" />
        <activity android:name=".Activity.Doraemon" />
        <activity android:name=".Activity.MotuPatlu" />
        <activity android:name=".Activity.TomJerry" />
        <activity
            android:name=".PlayerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|layoutDirection" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>